import { DateCvaDirective } from './date-cva.directive';

describe('DateCvaDirective', () => {
  it('should create an instance', () => {
    const directive = new DateCvaDirective();
    expect(directive).toBeTruthy();
  });
});
