import { TableFieldDirective } from './table-field.directive';

describe('TableFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TableFieldDirective();
    expect(directive).toBeTruthy();
  });
});
